export enum PlanStatus {
    active = "active",
    cancelled = "cancelled",
    expired = "expired",
}

export enum ResumeType {
    ATS = "ATS",
    REGULAR = "REGULAR",
}

export enum DraftType {
    RESUME = "RESUME",
    PORTFOLIO = "PORTFOLIO",
    CHATBOT = "CHATBOT",
}

export enum ChatRole {
    USER = "USER",
    ASSISTANT = "ASSISTANT",
}

